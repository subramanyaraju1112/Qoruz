import { styled } from '@stitches/react';

const ToolHeroStyled = styled('section', {
  '.bg-image': {
    width: '$full',
    height: 400,
    objectFit: 'cover',
  },
});

export default ToolHeroStyled;
